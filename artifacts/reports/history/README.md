# Historical Generated Reports

This directory is intentionally non-canonical generated history.

The MLB-first rebuild keeps canonical report artifacts under `artifacts/reports/mlb/`
and validation artifacts under `artifacts/validation/`. Do not commit generated
legacy league report outputs here; recover retired NBA/NHL history from git only
when explicitly needed.
