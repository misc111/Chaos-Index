# Legacy Staging Snapshots

The NBA and NHL JSON snapshots in this directory are retained for audit and
migration reference only. They are not part of the primary shipped staging
contract, which is MLB-only and declared in `../manifest.json`.
